{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "Main backend API URL from Render. Format: https://neon-farming-backend.onrender.com/api/v1",
      "required": true
    },
    "NEXT_PUBLIC_CHAT_API_URL": {
      "description": "Chat service HTTP API URL from Render. Format: https://neon-farming-chat.onrender.com/api/v1/chat",
      "required": true
    },
    "NEXT_PUBLIC_CHAT_WS_URL": {
      "description": "Chat service WebSocket URL from Render. Format: wss://neon-farming-chat.onrender.com/ws/chat",
      "required": true
    },
    "NEXT_PUBLIC_VAPID_PUBLIC_KEY": {
      "description": "VAPID public key for push notifications — same key set in backend VAPID_PUBLIC_KEY",
      "required": false
    }
  }
}
